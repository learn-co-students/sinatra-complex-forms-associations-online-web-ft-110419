<h1>Update Owner</h1>

<h2>Edits for <%= @owner.name %></h2>

<form method = "POST" action = "/owners/<%= @owner.id %>">
	<input type = "hidden" id = "hidden" name = "_method" value = "patch">
	<p>Edit Owner's Name: <input type="text" name="owner[name]"></p>
	<br>
	<p>Select an existing pet or create a new one below.</p>

	<h3>Existing Pets:</h3>

	<% @pets.each do |pet| %>
		<input type = "checkbox" name = "owner[pet_ids][]" value = "<%= pet.id %>" id="<%= pet.id %>" <%= 'checked' if @owner.pets.include?(pet) %>>
		<%= pet.name %>
		<br>
	<% end %>

	<h3>New Pet:</h3>
	<label>Pet Name:</label>
	<input type="text" name="pet[name]" id="pet_name">
	<br>
	<br>
	<input type="submit" value="Update Owner">
</form>